"""
input: Optional[TreeNode]
output: Optional[TreeNode]
Given the root of a binary tree "root". Invert the binary tree and return its root.

note:
----
could we swap recursively the left and right child

"""
