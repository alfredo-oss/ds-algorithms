from typing import List

# Initialize an array.
myArray = [1,2,3]

# Access an arbitrary element, where i is the index of the desired value.
i = 2 # Accesing the last element which is always located at the index = (length of the array) - 1
print(myArray[i])
